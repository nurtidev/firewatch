/** Действие с сервером из оперативного блока выезда: `key` — какая кнопка
 *  крутит спиннер, `fn` — сам запрос. Ошибка показывается одним баннером
 *  над разделами (см. CalloutOps), после успеха пакет перечитывается. */
export type RunAction = (key: string, fn: () => Promise<unknown>) => Promise<void>;
